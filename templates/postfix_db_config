user = {{ postfix_db_user }}
password = {{ postfix_db_password }}
hosts = {{ postfix_db_host }}
dbname = {{ postfix_db_name }}
table = {{ item.table }}
select_field = {{ item.select_field }}
where_field = {{ item.where_field }}
